---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gateway-tls
  namespace: gateway
spec:
  secretName: gateway-tls
  issuerRef:
    name: le-prod
    kind: ClusterIssuer
  dnsNames:
    - "${gatewayHostname}"
